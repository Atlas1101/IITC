const q1_resource =[2,4,6,8,10]

let sum =0
q1_resource.forEach((number) =>{number**2
    sum += number


}
)

let toprint = document.createElement("ul")
toprint.textContent(sum)
document.appendChild(toprint)



const hasStrongPassword = passwords.some(function(password) {
    return password. length > 8 //[a-zA-ZJ/. test(password) /tø-9J/.        
})
return password. length > 8 /[a-zA-ZJ/. test(password) /tø-9J/.
onsole. // true
onst allUnique = passwords. length
new Set(passwords). size;
onsole. log(allUnique); // true